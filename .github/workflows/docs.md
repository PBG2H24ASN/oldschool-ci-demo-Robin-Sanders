1. es startet app.sh in einer linux ubuntu umgebung
2. die pipeline überprüft ob app.sh das gewollte ergebniss liefert, dies kann genutzt werden um zu schauen ob ein prozess abgestürzt oder ob er erfolgreich gestartet wird
3. heute ist ein schwieriger tag für mich und ich habe viele kleine fehler gemacht die ich dann fixen musste
